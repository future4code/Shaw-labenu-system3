export type Student = {
    id: number,
    name: string,
    email: string,
    birthDate: Date,
    classId: number,
    hobby: string[]
}

export type instructorSkill = {
    id: number,
    name: string,
    email: string,
    birthDate: Date,
    classId: number,
    skill: string[]
}

export type Hobby = {
    id: number,
    name: string
}

export type Skill = {
    id: number,
    name: string
}